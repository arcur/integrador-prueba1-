<%# src/views/TechStore_Intranet_Usuario/cuenta_mis_tarjetas.ejs %>
<%# Reemplaza a mis_tarjetas.html %>

<%# --- Definir layout y página actual --- %>
<% layout('../layouts/cuenta_layout') -%>
<% locals.currentPage = 'tarjetas' %>

<%# --- Contenido específico --- %>
<div class="d-flex justify-content-between align-items-center mb-4">
     <h2 class="futuristic-title mb-0">Métodos de Pago Guardados <i class="bi bi-credit-card-fill"></i></h2>
     <button class="btn btn-primary btn-theme disabled"> <%# Deshabilitado %>
         <i class="bi bi-plus-lg"></i> Agregar Nueva Tarjeta (Próximamente)
     </button>
</div>

<%# --- CONTENIDO ESTÁTICO DE EJEMPLO --- %>
<%# En el futuro, aquí iterarías sobre una variable 'tarjetas' pasada por el controlador %>
<div class="row row-cols-1 row-cols-lg-2 g-4">

    <div class="col">
        <div class="card card-theme border-primary border-2 h-100"> <%# Usar card-theme %>
            <div class="card-body d-flex flex-column">
                <div class="d-flex justify-content-between align-items-start mb-3">
                    <div>
                        <i class="bi bi-credit-card-2-front-fill fs-1" style="color: var(--bs-primary-text-emphasis);"></i>
                        <span class="badge bg-primary ms-2">Predeterminada</span>
                    </div>
                    <%# Botón de opciones (deshabilitado) %>
                    <button class="btn btn-sm btn-outline-secondary border-0 disabled" type="button">
                        <i class="bi bi-three-dots-vertical"></i>
                    </button>
                </div>
                <h5 class="card-title">Visa **** **** **** 1234</h5>
                <p class="card-text text-white-50">Expira: 12/2028</p>
                <p class="card-text small mt-auto text-muted"> <%# Usar text-muted %>
                    Agregada el 20 Oct, 2025
                </p>
            </div>
        </div>
    </div>
     <%# Placeholder tarjeta 2 %>
      <div class="col">
        <div class="card card-theme h-100"> <%# Usar card-theme %>
            <div class="card-body d-flex flex-column">
                <div class="d-flex justify-content-between align-items-start mb-3">
                     <i class="bi bi-credit-card-2-front fs-1 text-muted"></i> <%# Usar text-muted %>
                    <button class="btn btn-sm btn-outline-secondary border-0 disabled" type="button">
                        <i class="bi bi-three-dots-vertical"></i>
                    </button>
                </div>
                <h5 class="card-title">Mastercard **** **** **** 5678</h5>
                <p class="card-text text-white-50">Expira: 08/2026</p>
                <p class="card-text small mt-auto text-muted"> <%# Usar text-muted %>
                   Agregada el 15 Sep, 2025
                </p>
            </div>
        </div>
    </div>
    <div class="col-12"> <p class="text-center text-muted mt-5">Función de Tarjetas no implementada aún.</p> </div>
</div>
<%# --- FIN CONTENIDO ESTÁTICO --- %>

